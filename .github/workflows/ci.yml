name: API Specimen - Continuous Integration

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  validation-and-tests:
    runs-on: ubuntu-latest

    steps:
      # 1. Descargar el código del repositorio
      - name: Checkout source code
        uses: actions/checkout@v4

      # 2. Configurar el entorno de Java 21 (Temurin)
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'
          # Le indicamos a la caché de Maven dónde está el pom.xml
          cache-dependency-path: 'API_Specimen/pom.xml'

      # 3. Darle permisos de ejecución al Maven Wrapper apuntando a la subcarpeta
      - name: Grant execute permission for mvnw
        run: chmod +x mvnw
        working-directory: API_Specimen

      # 4. Compilar y ejecutar las pruebas unitarias aisladas con Mockito en la subcarpeta
      - name: Run Unit Tests with Maven
        run: ./mvnw clean test
        working-directory: API_Specimen